export interface WhatUsersSay {
  image: string;
  comment: string;
  name: string;
  role: string;
}

export interface InAction {
  videoLink: string;
  thumbnail: string;
}
export interface FrequentlyAskedQuestions {
  question: string;
  answer: string;
}
